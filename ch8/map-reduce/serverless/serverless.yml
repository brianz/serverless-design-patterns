service: map-reduce

provider:
  name: aws
  runtime: python3.6
  memorySize: 1024
  timeout: 300
  region: ${env:AWS_REGION}
  environment:
    MAPREDUCE_SQS_ARN:
      Ref: SNSMapperTopic
    REDUCE_RESULTS_BUCKET: "brianz-${env:ENV}-mapreduce-results"
  iamRoleStatements:
    - Effect: "Allow"
      Action:
        - "s3:*"
      Resource:
        - "arn:aws:s3:::big-data-benchmark"
        - "arn:aws:s3:::big-data-benchmark/*"
        - "arn:aws:s3:::brianz-${env:ENV}-mapreduce-results"
        - "arn:aws:s3:::brianz-${env:ENV}-mapreduce-results/*"
    - Effect: "Allow"
      Action:
        - "sns:*"
      Resource:
        Ref: SNSMapperTopic
        #"arn:aws:sns:${self:provider.region}:*:${env:ENV}PyChainMiners"
    - Effect: Allow
      Action:
        - lambda:InvokeFunction
      Resource: "arn:aws:lambda:${env:AWS_REGION}:*:function:map-reduce-${opt:stage}-Reducer"

package:
  exclude:
    - .git/**
    - __pycache__/**
    - "**/__pycache__/**"
    - "*.pyc"
    - "*.swp"

resources:
  Resources:
    SNSMapperTopic:
      Type: AWS::SNS::Topic
      Properties:
        DisplayName: ${env:ENV} notification for mappers
        TopicName: ${env:ENV}MapReduceMapper

functions:
  Driver:
    handler: handler.driver
  Mapper:
    handler: handler.mapper
    events:
      - sns:
          topicName: ${env:ENV}PyChainMiners
          arn:
            Ref: SNSMapperTopic
  Reducer:
    handler: handler.reducer
  FinalReducer:
    handler: handler.final_reducer
    events:
      - s3:
          bucket: brianz-${env:ENV}-mapreduce-results
          event: s3:ObjectCreated:*


plugins:
  - serverless-prune-plugin

custom:
  prune:
    automatic: true
    number: 2
